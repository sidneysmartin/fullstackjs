// (reserved keyword): (variable name): (type of value)
let name = "John Doe";
let age = 30;
let isMarried = false;

console.log(`${name} is ${age} years old and is married: ${isMarried}`);

console.log(
  "Em JavaScript temos 3 formas de declarar variáveis: (var) (let) (const)"
);
console.log("Var - E a forma mais antiga tem escopo global");
console.log(
  "let` e `const` são limitadas ao bloco onde são definidas, enquanto `var` é visível na função inteira, SEMPRE USE (LET OU CONST)"
);
